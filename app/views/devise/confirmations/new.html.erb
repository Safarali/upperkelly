<section class="devise">
    <div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <div class="panel panel-default">
                    <div class="panel-body text-center">
                        <h2>Resend confirmation instructions</h2>
                        <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
                              <!-- <%= devise_error_messages! %> -->
                              <%= render 'shared/devisemessage' %>
                              <div class="form-group">
                                  <i class="fa fa-envelope" aria-hidden="true"></i>
                                  <%= f.email_field :email, autofocus: true, value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email), class: "form-control", placeholder: "Email" %>
                              </div>
                              <%= f.submit "Confirmation instructions", class: "hero-btn form-btn" %>
                        <% end %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
